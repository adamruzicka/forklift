- hosts: all
  become: true
  module_defaults:
    group/theforeman.foreman.foreman:
      username: admin
      password: changeme
      server_url: "https://localhost/"
      validate_certs: false
  vars:
    libvirt_tftp: true
  roles:
    - katello
    - libvirt
    - katello_provisioning
