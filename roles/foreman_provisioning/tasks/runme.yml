- hosts: all
  module_defaults:
    group/theforeman.foreman.foreman:
      username: admin
      password: changeme
      server_url: "https://localhost/"
      validate_certs: false
  tasks:
    - include_tasks: ./configure_centos_9.yml
