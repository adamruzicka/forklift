- hosts: all
  become: true
  remote_user: root
  module_defaults:
    group/theforeman.foreman.foreman:
      username: admin
      password: changeme
      server_url: "https://localhost/"
      validate_certs: false
  vars:
    libvirt_tftp: True
  roles:
    - role: luna_provisioning
